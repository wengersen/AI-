# AI 在工作中的实际应用 — 交互式汇报网站

## 在线访问

部署完成后，通过以下链接访问：
`https://你的用户名.github.io/ai-work-report/`

## 本地查看

双击 `index.html` 即可在浏览器中打开。

## 修改内容

编辑 `data/content.js` 文件即可更新所有展示内容。
- 图片放在 `assets/images/` 对应文件夹下
- 视频放在 `assets/images/` 或 `assets/videos/` 下
- 在配置文件中填写对应路径
